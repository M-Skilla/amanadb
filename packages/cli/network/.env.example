# Copy to .env in this same directory and fill in real values, OR run `amanadb harden`,
# which generates this file with strong random secrets automatically if it's missing.
# .env is gitignored — never commit real secrets here.

CA_ORG1_ADMIN_USER=admin
CA_ORG1_ADMIN_PASS=changeme

CA_ORG2_ADMIN_USER=admin
CA_ORG2_ADMIN_PASS=changeme

CA_ORDERER_ADMIN_USER=admin
CA_ORDERER_ADMIN_PASS=changeme

COUCHDB_ORG1_USER=admin
COUCHDB_ORG1_PASS=changeme

COUCHDB_ORG2_USER=admin
COUCHDB_ORG2_PASS=changeme
